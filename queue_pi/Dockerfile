# Dockerfile for Queue Pi (RabbitMQ)

# Use the official RabbitMQ base image
FROM rabbitmq:3-management

# Expose RabbitMQ ports
EXPOSE 5672 15672

# Enable the management plugin (already enabled by default in the rabbitmq:3-management image)
